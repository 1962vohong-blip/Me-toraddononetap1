# JVM Memory
org.gradle.jvmargs=-Xmx2048m -Xms512m

# Parallel builds (faster)
org.gradle.parallel=true
org.gradle.workers.max=4

# Daemon (keep JVM warm between builds)
org.gradle.daemon=true

# Compiler settings
org.gradle.java.version=21
